name: prestashop-1743

on:
  push:
    branches:
      - master
    paths:
      - prestashop-1.7/modules/frisbo_awb/**

permissions:
  contents: write

concurrency:
  group: prestashop-1743-release
  cancel-in-progress: false

jobs:
  php-compatibility:
    name: PHP ${{ matrix.php }} syntax
    runs-on: ubuntu-latest
    strategy:
      matrix:
        php:
          - "7.2"
          - "7.4"

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Check module PHP syntax
        shell: bash
        run: |
          set -euo pipefail

          docker run --rm \
            --volume "$GITHUB_WORKSPACE/prestashop-1.7/modules/frisbo_awb:/module:ro" \
            "php:${{ matrix.php }}-cli" \
            sh -ec 'find /module -type f -name "*.php" -exec php -l {} \;'

  prestashop-compatibility:
    name: PrestaShop ${{ matrix.prestashop }} / PHP ${{ matrix.php }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      max-parallel: 4
      matrix:
        # Oldest supported patch plus the two newest, each on PHP edges.
        include:
          - { prestashop: "1.7.4.3", php: "7.2" }
          - { prestashop: "1.7.4.3", php: "7.4" }
          - { prestashop: "1.7.8.10", php: "7.2" }
          - { prestashop: "1.7.8.10", php: "7.4" }
          - { prestashop: "1.7.8.11", php: "7.2" }
          - { prestashop: "1.7.8.11", php: "7.4" }

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Install module on PrestaShop
        shell: bash
        env:
          PRESTASHOP_VERSION: ${{ matrix.prestashop }}
          PHP_VERSION: ${{ matrix.php }}
        run: |
          set -euo pipefail
          ./prestashop-1.7/scripts/test-module-version.sh "$PRESTASHOP_VERSION" "$PHP_VERSION"

  release:
    name: Package and release frisbo_awb
    runs-on: ubuntu-latest
    needs:
      - php-compatibility
      - prestashop-compatibility

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Create PrestaShop module ZIP
        id: package
        shell: bash
        run: |
          set -euo pipefail

          release_tag="v$(date -u +'%Y-%m-%d_%H%M%S')"
          archive_name="frisbo_awb-${release_tag}.zip"

          mkdir -p build dist
          cp -a prestashop-1.7/modules/frisbo_awb build/frisbo_awb
          find build/frisbo_awb -type f \( -name '.DS_Store' -o -name '*.swp' \) -delete

          test -f build/frisbo_awb/frisbo_awb.php
          (cd build && zip -q -r "../dist/${archive_name}" frisbo_awb)
          sha256sum "dist/${archive_name}" > "dist/${archive_name}.sha256"

          echo "release_tag=${release_tag}" >> "$GITHUB_OUTPUT"
          echo "archive_name=${archive_name}" >> "$GITHUB_OUTPUT"

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ steps.package.outputs.release_tag }}
          ARCHIVE_NAME: ${{ steps.package.outputs.archive_name }}
        shell: bash
        run: |
          set -euo pipefail

          gh release create "$RELEASE_TAG" \
            "dist/$ARCHIVE_NAME" \
            "dist/$ARCHIVE_NAME.sha256" \
            --target "$GITHUB_SHA" \
            --title "PrestaShop 1.7 - $RELEASE_TAG" \
            --notes "Compatible with PrestaShop 1.7.4.3-1.7.8.11 and PHP 7.2-7.4. CI tests the oldest supported patch and two newest patches on the oldest and newest PHP runtimes." \
            --generate-notes
